name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - main

jobs:
  # Customize this workflow based on your project's language and tech stack
  
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Example for Node.js/TypeScript
      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #     cache: 'npm'
      # - run: npm ci
      # - run: npm run lint

      # Example for Python
      # - name: Setup Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.11'
      #     cache: 'pip'
      # - run: pip install -r requirements.txt
      # - run: black --check .
      # - run: flake8 .

      # Example for Go
      # - name: Setup Go
      #   uses: actions/setup-go@v5
      #   with:
      #     go-version: '1.21'
      #     cache: true
      # - run: go fmt ./...
      # - run: golangci-lint run

      - name: Placeholder lint step
        run: echo "Configure linting for your project"

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Example for Node.js/TypeScript
      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #     cache: 'npm'
      # - run: npm ci
      # - run: npm test
      # - name: Upload coverage
      #   uses: codecov/codecov-action@v4
      #   if: always()

      # Example for Python
      # - name: Setup Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.11'
      #     cache: 'pip'
      # - run: pip install -r requirements.txt
      # - run: pytest --cov=. --cov-report=xml
      # - name: Upload coverage
      #   uses: codecov/codecov-action@v4
      #   if: always()

      # Example for Go
      # - name: Setup Go
      #   uses: actions/setup-go@v5
      #   with:
      #     go-version: '1.21'
      #     cache: true
      # - run: go test -v -coverprofile=coverage.out ./...
      # - name: Upload coverage
      #   uses: codecov/codecov-action@v4
      #   if: always()

      - name: Placeholder test step
        run: echo "Configure testing for your project"

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Example for Node.js/TypeScript
      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #     cache: 'npm'
      # - run: npm ci
      # - run: npm run build
      # - name: Upload build artifacts
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: build
      #     path: dist/

      # Example for Go
      # - name: Setup Go
      #   uses: actions/setup-go@v5
      #   with:
      #     go-version: '1.21'
      #     cache: true
      # - run: go build -v ./...

      - name: Placeholder build step
        run: echo "Configure build for your project"
